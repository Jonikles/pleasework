# Server Configuration
server.port=8080

# Application Configuration
app.allowed-origins=http://localhost:8080
app.api.files-base-url=/api/files/

# Email Configuration
app.email.enabled=true
app.email.mock=true
app.email.from=noreply@tutoringplatform.com

# Logging Configuration
logging.level.root=INFO
logging.level.com.tutoringplatform=DEBUG
logging.level.com.tutoringplatform.observer.EmailService=DEBUG